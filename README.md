# cozycubes

A Rubik's Cube speedsolving timer, built with React, Vite, and Tailwind.

## Getting started

```
npm install
npm run dev
```

Hold `Space` to arm the timer (turns red, then green once ready), release to start, then press `Space` again to stop.
